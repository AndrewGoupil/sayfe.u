<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="twoeleven.sayfe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/SayfeTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".LessonMenuActivity" />
        <activity android:name=".SupportActivity" />
        <activity android:name=".Lesson11Activity" />
        <activity android:name=".Lesson12Activity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".studentNumberActivity"></activity>
    </application>

</manifest>
